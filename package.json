{
  "name": "simple-crdt-editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:back": "cargo watch -q -c -x \"run --bin server\" -w server",
    "build": "tsc && vite build && cargo build --release",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "fast-diff": "^1.2.0",
    "y-websocket": "^2.0.4",
    "yjs": "^13.5.27"
  }
}
